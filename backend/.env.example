# Database
DATABASE_URL=postgresql://username:password@localhost:5432/kucing_comel

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this

# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Server
PORT=5000
NODE_ENV=development

# Email Configuration (for order confirmations)
# Leave SMTP_HOST empty or as 'your-smtp-host' for console logging in dev mode
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-email-password
SMTP_FROM_EMAIL=noreply@kucingcomel.com
SMTP_FROM_NAME=Kucing Comel
