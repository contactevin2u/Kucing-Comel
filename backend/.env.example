# Database
DATABASE_URL=postgresql://username:password@localhost:5432/kucing_comel

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this

# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Server
PORT=5000
NODE_ENV=development

# Email Configuration (Gmail SMTP - for order confirmations & shipping notifications)
# 1. Go to Google Account → Security → 2-Step Verification → App Passwords
# 2. Generate an App Password for "Mail"
# 3. Use your Gmail address as SMTP_USER and the App Password as SMTP_PASSWORD
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=petpalshubsb@gmail.com
SMTP_PASSWORD=your-gmail-app-password
SMTP_FROM_EMAIL=petpalshubsb@gmail.com
SMTP_FROM_NAME=PetPals Hub
